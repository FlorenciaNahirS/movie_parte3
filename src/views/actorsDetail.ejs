<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>
    <%= actor.first_name +' '+ actor.last_name %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <figure>
      <img src="/img/logo-DH.png" alt="Logo Digital House" />
    </figure>
    <h1><%= actor.first_name  +' '+ actor.last_name %></h1>
    <br />
    <p>RATING: <%= actor.rating %></p>
    <br>
    <p>FAVORITE MOVIE: <%= favoriteMovie %></p>
    <br>
    <% if (movies.length > 0) { %>
     <p>FILMOGRAFY:</p>
      <ul style="margin: 0 0 0 30px;">
          <% movies.forEach(movie => { %>
              <li> <%= movie %> </li>
          <% }) %>
      </ul>
    <% } %>
    
    <br />
    <br />
    <h3><a href="/actors/edit/<%= actor.id %>">Editar Actor</a></h3>
    <br />
    <h3><a href="/actors/delete/<%= actor.id %>">Eliminar Actor</a></h3>
    <br />
    <br />
    <a href="/actors">Ver listado de Actores</a>
  </body>
</html>